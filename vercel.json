{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "bunVersion": "1.4.x",
  "installCommand": "cd archify && bun install",
  "routes": [
    { "src": "/api/server/(.*)", "dest": "/api/server" }
  ]
}
